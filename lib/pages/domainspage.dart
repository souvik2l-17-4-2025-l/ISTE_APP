import 'package:flutter/material.dart';
class domainspage extends StatelessWidget {
  const domainspage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text(" Domains Page")),
    );
  }
}
